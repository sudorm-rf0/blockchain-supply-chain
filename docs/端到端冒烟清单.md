# 端到端冒烟清单（真实用户路径逐项）

> 目的：上线前用一条命令跑通真实用户路径，确保"真实可用"
> 主脚本：`scripts/smoke-e2e.mjs`（API + 链上冒烟，520 行，断言见 results 汇总）
> 执行：`bash scripts/verify-deployment.sh`（`RUN_SMOKE=1` 时调用）或直接 `node scripts/smoke-e2e.mjs`
> 状态：✅ 已自动化 · 🟡 部分/需扩展 · ⬜ 待补（手动）

---

## 0. 冒烟前置

- [ ] staging/主网已部署（合约 + 后端三服务 + indexer + 前端）
- [ ] `USDC_MINT` / `LP_MINT` 已配置（订单资金流启用）
- [ ] 资金池已初始化、供应链注册中心已初始化
- [ ] `SKIP_SUPPLY_CHAIN` 未设置（默认启用供应链冒烟）

## 1. 用户路径清单

### A. 认证与账户
| # | 路径 | 步骤 | 预期 | 验证 | 状态 |
|---|------|------|------|------|------|
| A1 | 注册 | POST /api/auth/register | 返回 accessToken | smoke `results.register` | ✅ |
| A2 | 登录 | POST /api/auth/login（admin） | token 有效 | smoke adminLogin | ✅ |
| A3 | 改密 | change-password → 重登 | 新密码生效 | smoke | ✅ |
| A4 | 前端登录页 | 浏览器输入凭据 | 进入仪表盘 | 手动/Playwright | ⬜ |

### B. 单据与存证
| # | 路径 | 步骤 | 预期 | 验证 | 状态 |
|---|------|------|------|------|------|
| B1 | 文件上传 | POST /api/files | 生成 file.id | smoke `results.upload` | ✅ |
| B2 | 存证 | /api/files/:id/attest → confirm | documentPda 上链 | smoke `results.attest` | ✅ |
| B3 | 前端上传/审批页 | UI 上传→审批→存证 | 状态流转 | 手动 | ⬜ |

### C. 订单全流程（买方/管理员/卖方）
| # | 路径 | 步骤 | 预期 | 验证 | 状态 |
|---|------|------|------|------|------|
| C1 | 创建订单 | 建单（买方付 30% 首付） | 订单 PENDING | smoke | ✅ |
| C2 | 放款 | 管理员放款 70% | FUNDED，池 active/escrow 更新 | smoke | ✅ |
| C3 | 物流推进 | advance ×3 | DELIVERED | smoke | ✅ |
| C4 | 释放托管 | 给卖方 | REPAYING，卖方收款 | smoke | ✅ |
| C5 | 还款 | 买方还 70%+费用 | SETTLED，费用三分（平台/LP/返利） | smoke `results.tradeLifecycle` | ✅ |
| C6 | 前端订单流 | UI 建单→看板→还款 | 状态/资金显示 | 手动 | ⬜ |

### D. 违约
| # | 路径 | 步骤 | 预期 | 验证 | 状态 |
|---|------|------|------|------|------|
| D1 | 融资期违约 | default（未释放） | DEFAULTED，托管回池 | smoke `results.defaultScenario` | ✅ |
| D2 | 还款期违约 | 账期到期 default | 保险/首损核销 | smoke（部分） | 🟡 |

### E. LP 与资金池
| # | 路径 | 步骤 | 预期 | 验证 | 状态 |
|---|------|------|------|------|------|
| E1 | LP 存款 | deposit_pool | 链上铸 LP（C-01） | 手动/脚本扩展 | 🟡 |
| E2 | LP 赎回 | redeem_lp | 按 equity_base 兑付、窗口/首损保护 | 手动/脚本扩展 | 🟡 |
| E3 | 分红 | distribute_dividends | 台账记录、pending 递减 | 手动/脚本扩展 | 🟡 |
| E4 | 首损注资/消耗 | deposit_first_loss → default | 首损优先吸收 | 手动 | 🟡 |

### F. 供应链
| # | 路径 | 步骤 | 预期 | 验证 | 状态 |
|---|------|------|------|------|------|
| F1 | 注册中心初始化 | initialize_registry | registry.admin 正确 | smoke | ✅ |
| F2 | 授权供应商 | authorize_supplier | Supplier PDA | smoke | ✅ |
| F3 | 注册商品 | register_product | Product 账户 | smoke `results.supplyChainRegister` | ✅ |
| F4 | 未授权拒绝 | 非授权注册 | Unauthorized | smoke `results.supplyChainRejectUnauthorized` | ✅ |
| F5 | 撤销/商品失效 | revoke_supplier/revoke_product | 状态失效 | smoke（部分） | 🟡 |

---

## 2. 执行与断言

```bash
# 一键（verify-deployment 内置 RUN_SMOKE=1 时）
RUN_SMOKE=1 BACKEND_URL=... TRADE_URL=... POOL_URL=... INDEXER_URL=... \
  USDC_MINT=... LP_MINT=... bash scripts/verify-deployment.sh

# 或直接跑 smoke
node scripts/smoke-e2e.mjs   # 需 BACKEND_URL/TRADE_URL/POOL_URL + USDC/LP_MINT + 已初始化池
```

smoke-e2e.mjs 的**硬断言**（缺失即 FAIL）：
`results.register` / `results.upload` / `results.attest` / `results.tradeLifecycle`（=SETTLED）/ `results.defaultScenario`（=DEFAULTED）/ `results.supplyChainRegister` / `results.supplyChainRejectUnauthorized`

## 3. 缺口与补充建议

| 缺口 | 影响 | 补充方式 |
|------|------|----------|
| **LP 存取/分红（E1–E3）** | 资金池核心能力未冒烟 | 扩展 smoke-e2e.mjs：新增 LP 钱包 deposit→redeem→distribute 段（断言 lp 余额/redemptionPrice/pending）；或独立 `smoke-lp.mjs` |
| **前端 UI 路径（A4/B3/C6）** | 用户实际使用未验证 | 用 Playwright/agent-browser 录制登录→看板→建单→还款脚本；或上线前人工逐项（勾选本清单） |
| **还款期违约保险/首损（D2）** | 违约保险路径 | smoke 需构造账期到期（warp 或短账期测试池），补 default REPAYING 段 |
| **撤销/商品失效（F5）** | 供应链治理 | 补 revoke_supplier/revoke_product 后注册被拒断言 |

## 4. 上线前执行要求

- [ ] A–F 全部 ✅ 项在 staging 跑通（`smoke-e2e.mjs` 全断言 PASS）
- [ ] 前端 UI 路径（A4/B3/C6）人工/自动化逐项通过
- [ ] LP 存取 + 分红（E1–E3）冒烟通过（脚本扩展后）
- [ ] 结果记录到上线日志（含 trade 数、tx 数、错误率）

---

*生成：2026-08-08 · 覆盖=smoke-e2e.mjs 现状；缺口建议纳入下一迭代*
