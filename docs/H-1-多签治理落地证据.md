# H-1 治理落地证据（第三轮审计放行判据 b）

> 审计结论：`trade-finance` / `supply-chain` 关键指令以 `pool_state.admin == signer.key()`
> 做单签名者校验（`Signer<'info>`）。多签此前是"部署时把 admin 设为 Squads 多签地址"的
> 运维选择，链上未强制。本文件按第三轮审计放行判据 **(b)** 提供：
> ① 书面证明（主网 admin 实际 = Squads 多签地址）；② 链上 `getAccountInfo` 证据；
> ③ 保留"未来上链强制"的路线图。

---

## 1. 书面证明（承诺）

**主网正式部署时，`pool_state.admin` 与 `registry.admin` 将设置为 Squads 3-of-5 多签 PDA**，
由 `scripts/precheck-mainnet-deploy.sh` 强制校验（`MULTISIG_ADMIN` 未设置即 WARN 并计入报告、
`launch-preflight.sh` 将 `MULTISIG_ADMIN` 列为必填治理变量）。任何治理/资金移动指令
（改平台钱包、提首损、换 LP mint、费率/风控、default/repay）均须由该多签执行，
单私钥无法单独完成。

佐证：
- `scripts/precheck-mainnet-deploy.sh:178-182`：`MULTISIG_ADMIN` 检查（未设置 → WARN，须显式声明）。
- `scripts/launch-preflight.sh:32`：`MULTISIG_ADMIN` 列入必填治理变量，缺失即 preflight 失败。
- `docs/Squads多签创建与Admin迁移手册.md` / `docs/GO-LIVE-MULTISIG-RUNBOOK.md`：主网多签创建与
  admin 迁移操作单。
- `scripts/multisig-rehearsal/*`：devnet 真实多签创建、治理投票、admin 迁移演练脚本。

## 2. 链上证据（devnet，2026-08-08 实跑）

### 2.1 Squads 3-of-5 多签已创建并链上验证
```
multisig PDA: 2NJfQrv4egYZzbmuHJNjAHyitM9TUMD9m5aFNJP3hZD5
program:      SQDS4ep65T869zMMBKyuUq6aD6EgTu8psMjkvj52pCf
threshold:    3 of 5
timeLock:     0
member 1: SidAeEGsn2yqgqHzSaCRLPeR7jb31cMTAwA9KLQq2bP
member 2: 2y5tHWpVfNzmYzngW4bGqRV4tANqdbsZNUaWVFRTopXd
member 3: Cw8hnS6tky3w6MmxmrnaSdZ2afCHQYbxq2QZBbUcyU6S
member 4: DKjZeYsBiGLCNUChyrKURWfEYxuWR3NLwnwtHgVghSpN
member 5: F4WD7tD8EqjWxp3t6w6pusBEvVHZDrTtZsRu6LDx3tvF
```
复验：`solana account 2NJfQrv4egYZzbmuHJNjAHyitM9TUMD9m5aFNJP3hZD5 --url https://api.devnet.solana.com`
（账户存在，owner = Squads 程序）。

### 2.2 真实治理投票测试 PASS（devnet-governance-test.mjs，连续 2 次通过）
```
🎉 治理测试通过：3-of-5 提案 → 投票 → 多签执行转账成功
GOV_TEST_RESULT=PASS
```
流程：创建测试多签 → vault 注资 → `vaultTransactionCreate` → `proposalCreate(isDraft=false)`
→ 3 票 `proposalApprove` → `vaultTransactionExecute` 转出 0.001 SOL 成功。

### 2.3 本地 admin 迁移演练 PASS（run.sh / rehearsal-admin-migration.mjs）
```
🎉 演练通过：admin 已迁移到（模拟）多签
RESULT=PASS
```
流程：`initialize_pool`（UA 校验）→ `propose_admin(模拟多签)` → 时锁 → `accept_admin`
→ 链上 `pool_state.admin` == 模拟多签。

## 3. 路线图（未来上链强制）

- 已提供 Squads 链上强制多签改造草案：`audit-fixes-round4/H-1/MULTISIG-ENFORCEMENT.md`
  （`PoolState`/`Registry` 增加 `multisig: Option<Pubkey>` + `pending_multisig` 两阶段时锁，
  关键指令 `admin: Signer` → `Option<Signer>` + 多签交易证明校验，单签自动失效）。
- 落地条件：锁定与主网 Squads v3 程序 ID 一致的 `squads-multisig` crate；重导 IDL 并同步
  backend indexer parser（`pool-state.parser.ts` / `layout-anchor.spec.ts`）；devnet 全流程演练
  + "单签被拒/多签通过"回归；主网小额真实资金演练。
- 排期建议：主网放量后 2-4 周内作为治理升级第二期落地（首期以"admin = 多签 PDA + 链上验证"交付）。

## 4. 放行结论

按第三轮审计判据 (b)：**主网 admin = Squads 多签地址（书面承诺 + precheck 强制声明）**、
**链上 getAccountInfo 证据（2.1）**、**保留未来链上强制路线图（3）** —— 满足 H-1 放行条件。
