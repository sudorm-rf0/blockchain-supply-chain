# 项目上线优化清单（尽快上线 · 真实可用）

> 目标：识别并消除上线阻塞与真实可用性风险
> 现状基线：合约 DFR-0148 无保留通过、CI 全绿、上线工具链（手册/脚本/清单）就绪
> 分级：**P0 = 上线阻塞** · **P1 = 上线前应完成** · **P2 = 可后置优化**

---

## P0 · 上线阻塞（必须在接真实资金前完成）

| # | 项 | 现状 | 优化动作 | 对接 |
|---|----|------|----------|------|
| 1 | **治理前置执行** | 手册/脚本已就绪，未执行 | 创建 Squads ≥3/5 多签；部署 UA→多签；生产构建验证无 `test-build`；LP mint authority 交多签；precheck 全 PASS | `docs/Squads多签创建与Admin迁移手册.md`、`scripts/launch-preflight.sh` |
| 2 | **首损池注资 + 经济参数确认** | `fee_apy_bps=670` 为默认值；首损未注入 | 业务/风控确认目标违约率并校准费率；按 SOP 注入首损（在途 6%）；对外披露 | `docs/H-04-费率重构方案.md`、`docs/首损池注资与运营SOP.md` |
| 3 | **链下生产部署 + 全量验证实跑** | 验证脚本已串联为 `scripts/staging-verify.sh`（一条命令跑 健康检查→部署验证→监控→备份→回滚→对账），CI 已含编排预演 | 在真实 staging 执行 `staging-verify.sh` 全 PASS；逐步替换为生产环境复跑 | `docs/链下系统上线检查清单.md` |
| 4 | **生产基础设施** | 域名/SSL/付费 RPC 未就绪 | 申请域名 + TLS；主网付费 RPC（Helius/QuickNode，多 Key 轮询）；VPS/k8s 生产资源配置 | `infra/config/production.env.example`、`deploy/vps/`、`k8s/` |

## P1 · 上线前应完成（真实可用性）

| # | 项 | 现状 | 优化动作 | 影响 |
|---|----|------|----------|------|
| 5 | **依赖漏洞修复** | ✅ 已修复（2026-08-08）：`serialize-javascript`→7.0.7（RCE）、`diff`→8.0.4（DoS）；**生产 audit 归零**；完整 audit 仅剩 `bigint-buffer`（**无修复版、dev-only**，经 `@solana/spl-token` 仅测试用，不影响生产） | 安全基线 |
| 6 | **可复现构建** | ✅ 已实现（2026-08-08）：`scripts/reproducible-build.sh`（固定工具链两次构建哈希一致）+ `verify-reproducible.sh`（链上字节码对比）+ CI contracts job 集成；solana-verify 作为官方替代可选 | 上线可信度 |
| 7 | **端到端真实路径冒烟** | ✅ 清单 `docs/端到端冒烟清单.md`（A–F 六类路径）+ `smoke-e2e.mjs`（认证/存证/订单/违约/供应链）+ `smoke-lp.mjs`（LP 存取/分红）；前端 UI 路径待人工/自动化 | 真实可用 |
| 8 | **监控告警实战** | 规则已配，未注入故障演练 | 故障注入（停服务/断 RPC/模拟违约）验证告警触发与恢复 | 可运维 |
| 9 | **密钥管理 SOP 落地** | 手册有说明，未演练 | 冷钱包/多签私钥/Program keypair 离线备份流程执行 + 恢复演练 | 防丢失 |
| 10 | **数据保留/隐私合规实现** | 文档存在（DATA-RETENTION/PRIVACY-TERMS） | 核对代码实现（审计日志保留、隐私删除流程）与文档一致 | 合规 |
| 11 | **容量/负载验证** | LOAD-TEST-REPORT 存在，需按目标量级复核 | 按上线预期并发压测后端/indexer/RPC；确认 k8s resources | 防雪崩 |

## P2 · 可后置优化

| # | 项 | 说明 |
|---|----|------|
| 12 | 合约模块化 | lib.rs 已拆 events.rs，可继续拆 instructions/（纯代码质量） |
| 13 | 模糊测试 | 已加 proptest 属性测试，可加 trident fuzzing（状态空间覆盖） |
| 14 | 前端体验 | UI 细节、移动端、无障碍、加载态 |
| 15 | 性能优化 | indexer 批处理、Redis 缓存命中、RPC 请求合并 |
| 16 | 文档双语 | OPERATIONS/DEPLOYMENT 已有 .en.md，可补齐其余 |
| 17 | 机构背书 | DFR=元信链研资质问题，对外展示附资质或如实标注 |

---

## 整体路径（尽快上线）

```
【本周可并行推进】
  A. P0-1 治理前置（多签创建 → launch-preflight 全 PASS）
  B. P0-2 经济参数确认（业务/风控决策 + 首损预算）
  C. P1-5 依赖漏洞修复（我来做，快）
  D. P0-3 staging 部署 + 全量验证实跑（部署脚本已就绪）

【上线日】
  precheck 全 PASS → deploy-mainnet.sh --upgrade-authority <多签> 
  → 多签执行 initialize → 小额冒烟 → 链下清单逐项 → 放量

【上线后 24h】
  对账/违约/首损水位/RPC 稳定性重点监控
```

## 我可以立即帮忙的（按收益排序）

1. **P1-5 依赖漏洞修复**（`bigint-buffer`/`serialize-javascript` 升级或 override，audit 归零）
2. **P1-6 可复现构建脚本**（solana-verify 流程 + CI 集成）
3. **P0-3 部署验证跑批**（把 verify/monitoring/backup/reconcile 串成一条 staging 验证命令）
4. **P1-7 端到端冒烟清单**（真实用户路径逐项）

---

*生成：2026-08-08 · 基于当前 main（DFR-0148 通过、CI 全绿）+ 依赖/测试/部署扫描*
