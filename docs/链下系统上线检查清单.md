# 链下系统上线检查清单

> 配套：`docs/主网上线Go-Live检查清单.md`（合约侧）+ `docs/上线准备-DFR-0148通过.md`（合约前置）
> 本清单覆盖**后端 / 前端 / indexer / k8s / 监控 / 备份 / 灾备**，每个检查项对接现有脚本或 CI job。
> 状态：**上线前逐项勾选**；所有脚本见 `scripts/`，CI 见 `.github/workflows/ci.yml`

---

## 0. 组件与服务清单（对照）

| 组件 | 说明 | 端口（k8s Service） |
|------|------|---------------------|
| backend（主服务） | API/认证/文件/供应链 | 3001 |
| trade-service | 交易订单/放款/还款 | 3004（CI 约定） |
| pool-service | 资金池/LP | 3005（CI 约定） |
| indexer-service | 链上账户解析/快照 | 3003（CI 约定） |
| frontend | Next.js Web | 3000 |
| postgres / redis | 主数据 / 缓存 | 5432 / 6379 |
| prometheus / grafana / alertmanager / blackbox | 可观测 | 9090 / 3000 / 9093 / 9115 |

---

## 1. 构建与 CI 就绪

- [ ] **CI 全绿**：`gh run list` 最近一次 main 运行无 failure（contracts/backend/frontend/e2e/docker/backup-drill/monitoring）
- [ ] 后端镜像可构建：`docker build --target backend-runner -f Dockerfile.multistage .`（CI docker job 已覆盖）
- [ ] 前端镜像可构建：`docker build --target frontend-runner -f Dockerfile.multistage .`
- [ ] 合约生产构建**不带 `test-deployer`**：`anchor build` + `strings target/deploy/*.so | grep 3rF9fK7`（应无输出）
- [ ] 版本号/镜像 tag 策略确认（回滚演练依赖 `PREVIOUS_TAG`）

## 2. 环境与配置就绪

- [ ] `infra/config/production.env.example` 复制为 `production.env` 并填齐：
  - `SOLANA_RPC_URL`（主网付费档，Helius/QuickNode）
  - `USDC_MINT` / `LP_MINT`（LP authority 已交多签）
  - `DATABASE_URL` / `REDIS_URL` / `JWT_SECRET`（强随机）/ `ALLOWED_ORIGIN`
  - 治理前置：`UPGRADE_AUTHORITY_PLAN` / `MULTISIG_ADMIN` / `INITIAL_ADMIN_DELAY`
- [ ] 三个后端服务与 indexer 的 `TRADE_FINANCE_PROGRAM_ID` / `SUPPLY_CHAIN_PROGRAM_ID` / `LP_MINT` 与链上一致（见第 7 节）
- [ ] `REPAYMENT_NOTIFY_URL`（还款通知）指向真实风控/客户系统

## 3. 部署与验证（VPS 或 k8s）

- [ ] VPS：`deploy/vps/.env.app` 已配置，`bash scripts/deploy-vps.sh --rehearsal` 本地排练通过
- [ ] 正式部署：`bash scripts/deploy-vps.sh`（构建+启动+迁移+健康检查）
- [ ] k8s：`kubectl apply -f k8s/` 全部资源（backend/trade/pool/indexer/frontend/redis/postgres/prometheus/grafana/alertmanager/blackbox/ingress/network-policies）
- [ ] **部署后一键验证**：`bash scripts/verify-deployment.sh`（服务健康/就绪、指标、关键接口）全 PASS
- [ ] **健康检查**：`bash scripts/health-check.sh`（后端服务 + Solana RPC + 关键接口）全 PASS
- [ ] Prisma 迁移：`pnpm exec prisma migrate deploy` 成功且无 pending

## 4. 监控与告警就绪

- [ ] **监控验证**：`bash scripts/verify-monitoring.sh` 全 PASS（alert rules、/metrics、Prometheus/Alertmanager/Grafana 连通）
- [ ] 告警规则覆盖：违约事件、暂停/恢复、管理员轮换、首损池水位、对账失败、RPC 429
- [ ] Grafana Dashboard（`infra/grafana/dashboards/supply-chain.json`）加载正常，关键面板可见
- [ ] 告警路由：Alertmanager 已配置（`k8s/alertmanager-config.yaml`），Webhook/邮件可达
- [ ] Blackbox exporter 探活（`infra/blackbox`）覆盖所有服务端点

## 5. 备份与灾备

- [ ] **备份演练**：`bash scripts/db-backup-restore.sh drill` 通过（backup → restore → verify 全链路）
- [ ] 定时备份：k8s `postgres-backup-cronjob.yaml` 已部署（含备份保留策略）
- [ ] 备份存储位置/加密确认（异地/对象存储，防单点）
- [ ] **回滚演练**：`PREVIOUS_TAG=<上一版> bash scripts/rollback-drill.sh` 通过（dry-run → 真实 undo）
- [ ] 灾备 RTO/RPO 确认（备份频率 → RPO；恢复流程 → RTO），`INCIDENT-RUNBOOK.md` 已更新

## 6. 数据一致性与对账

- [ ] **链上/DB 对账**：`bash scripts/reconcile.sh --json` 无差异（exit 0）
- [ ] 对账加入 cron：每小时 `reconcile.sh`，失败触发告警（脚本已内置 webhook 用法）
- [ ] indexer 布局锚定：`pool-state.parser.ts` / `trade-deal.parser.ts` 与合约账户布局一致（CI backend job 的 layout-anchor.spec 覆盖）
- [ ] 首损池/资金池字段（first_loss_reserve 等）在 indexer 快照中正确解析

## 7. 安全与合规

- [ ] `k8s/network-policies.yaml` 已应用（服务间最小连通）
- [ ] JWT → httpOnly Cookie 认证、CSP 上报（`/metrics` csp_violations_total）已验证
- [ ] 敏感配置走 Secret（`backend-secrets`），不落明文仓库
- [ ] `scripts/scan-secrets.sh` 通过（仓库/历史无明文密钥）
- [ ] 数据保留策略（`docs/DATA-RETENTION.md`）与隐私条款（`docs/PRIVACY-TERMS-TEMPLATE.md`）确认
- [ ] 审计日志保留（`audit-retention`）配置符合合规要求

## 8. 容量与性能

- [ ] 压测报告（`docs/LOAD-TEST-REPORT.md`）覆盖峰值场景，结果达标
- [ ] 成本测算（`docs/COSTS.md`）按 5-tx/day 低量场景核对，确认预算
- [ ] RPC 配额充足（付费档 + 多 Key 轮询，防 429）
- [ ] Postgres/Redis 连接池与资源限制（k8s requests/limits）按容量设置

## 9. 上线当日流程（T-0）

- [ ] 按 `docs/GO-LIVE-RUNBOOK.md` 执行（部署顺序：合约 → 后端 → indexer → 前端 → 监控）
- [ ] 值班就位：`docs/ONCALL-SOP.md` 责任人明确，告警通道测试
- [ ] 小额真实资金冒烟：`node scripts/smoke-e2e.mjs`（创建订单 → 放款 → 还款）通过
- [ ] 首损池注资（在途 6%）并对外披露规模
- [ ] 上线后 24h 重点监控：对账、违约告警、RPC 稳定性、错误率

---

## 脚本与文档速查

| 用途 | 脚本/文档 |
|------|-----------|
| 部署后验证 | `scripts/verify-deployment.sh` |
| 健康检查 | `scripts/health-check.sh` |
| 监控验证 | `scripts/verify-monitoring.sh` |
| 备份/恢复演练 | `scripts/db-backup-restore.sh drill` |
| 回滚演练 | `scripts/rollback-drill.sh` |
| 链上/DB 对账 | `scripts/reconcile.sh` |
| 密钥扫描 | `scripts/scan-secrets.sh` |
| 部署（VPS） | `scripts/deploy-vps.sh` |
| 部署（k8s） | `k8s/*.yaml` |
| 运维/监控/应急 | `docs/OPERATIONS.md`、`docs/MONITORING.md`、`docs/INCIDENT-RUNBOOK.md` |

*生成：2026-08-08 · 对接现有脚本与 CI job · 与 `主网上线Go-Live检查清单.md` 互补（合约侧 + 链下侧）*
