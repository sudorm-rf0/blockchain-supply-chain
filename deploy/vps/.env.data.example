# ================= VPS-B（数据机）环境变量 =================
# 复制为 .env.data 并修改。docker compose -f data-compose.yml 读取。
POSTGRES_USER=supply
POSTGRES_PASSWORD=CHANGE_ME__strong_pg_password
POSTGRES_DB=supply_chain
REDIS_PASSWORD=CHANGE_ME__strong_redis_password
BACKUP_RETENTION_DAYS=7
