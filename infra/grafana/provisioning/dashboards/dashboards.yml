apiVersion: 1

providers:
  - name: supply-chain
    orgId: 1
    folder: supply-chain
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
