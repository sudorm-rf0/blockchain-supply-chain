apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: supply-chain
spec:
  type: ClusterIP
  selector:
    app: supply-chain
    tier: backend
  ports:
    - name: http
      port: 3001
      targetPort: http
